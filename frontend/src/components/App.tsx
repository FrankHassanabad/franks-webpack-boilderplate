import React from 'react';
import './app.scss';
import { Example } from './example';

/**
 * Example application which returns an example HTML element.
 * @returns
 */
export const App = () => {
  return <Example />;
};
